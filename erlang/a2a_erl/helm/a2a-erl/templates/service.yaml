apiVersion: v1
kind: Service
metadata:
  name: {{ include "a2a-erl.fullname" . }}
  labels:
    {{- include "a2a-erl.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a2a-erl.selectorLabels" . | nindent 4 }}
