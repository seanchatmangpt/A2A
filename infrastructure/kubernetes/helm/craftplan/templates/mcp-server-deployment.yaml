{{- if .Values.mcpServer.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "craftplan.fullname" . }}-mcp
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "craftplan.labels" . | nindent 4 }}
    app.kubernetes.io/name: mcp-server
  annotations:
    {{- if .Values.global.deploymentStrategy }}
    deployment.kubernetes.io/revision: "{{ .Values.global.deploymentStrategy.revision | default 1 }}"
    {{- end }}
    {{- if .Values.mcpServer.annotations }}
    {{- toYaml .Values.mcpServer.annotations | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ include "craftplan.replicaCount" . | default .Values.mcpServer.replicas }}
  {{- if .Values.mcpServer.strategy }}
  strategy:
    {{- toYaml .Values.mcpServer.strategy | nindent 4 }}
  {{- else }}
  strategy:
    type: {{ .Values.global.deploymentStrategy.type | default "RollingUpdate" }}
    rollingUpdate:
      maxSurge: {{ .Values.global.deploymentStrategy.rollingUpdate.maxSurge | default "25%" }}
      maxUnavailable: {{ .Values.global.deploymentStrategy.rollingUpdate.maxUnavailable | default "0" }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "craftplan.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/name: mcp-server
  template:
    metadata:
      labels:
        {{- include "craftplan.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/name: mcp-server
        app.kubernetes.io/component: mcp-server
        {{- if .Values.mcpServer.podLabels }}
        {{- toYaml .Values.mcpServer.podLabels | nindent 8 }}
        {{- end }}
      annotations:
        {{- if .Values.mcpServer.podAnnotations }}
        {{- toYaml .Values.mcpServer.podAnnotations | nindent 8 }}
        {{- end }}
        checksum/config: {{ include "craftplan.config" . | sha256sum }}
        checksum/secrets: {{ include "craftplan.secret" . | sha256sum }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "8090"
        prometheus.io/path: {{ include "craftplan.metricsPath" . }}
        {{- if .Values.mcpServer.monitoring.enabled }}
        kubernetes.io/monitoring: "true"
        {{- end }}
    spec:
      {{- if .Values.mcpServer.priorityClassName }}
      priorityClassName: {{ .Values.mcpServer.priorityClassName }}
      {{- end }}
      {{- if .Values.mcpServer.serviceAccount.name }}
      serviceAccountName: {{ include "craftplan.serviceAccountName" . }}
      {{- end }}
      {{- include "craftplan.imagePullSecret" . | nindent 6 }}
      {{- if .Values.mcpServer.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.mcpServer.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.mcpServer.tolerations }}
      tolerations:
        {{- toYaml .Values.mcpServer.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.mcpServer.affinity }}
      affinity:
        {{- toYaml .Values.mcpServer.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.global.costOptimization.spotInstances.enabled }}
      nodeSelector:
        {{- toYaml .Values.global.costOptimization.spotInstances.nodeSelector | nindent 8 }}
      tolerations:
        - key: "spot"
          operator: "Equal"
          value: "true"
          effect: "NoSchedule"
      {{- end }}
      securityContext:
        {{- include "craftplan.securityContext" . | nindent 8 }}
      containers:
        - name: mcp-server
          image: "{{ .Values.mcpServer.image.repository }}:{{ .Values.mcpServer.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.mcpServer.image.pullPolicy }}
          securityContext:
            {{- include "craftplan.securityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: 8090
              protocol: TCP
          {{- if .Values.mcpServer.livenessProbe }}
          livenessProbe:
            {{- toYaml (include "craftplan.livenessProbe" .) | nindent 12 }}
          {{- end }}
          {{- if .Values.mcpServer.readinessProbe }}
          readinessProbe:
            {{- toYaml (include "craftplan.readinessProbe" .) | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.mcpServer.env }}
            {{- toYaml .Values.mcpServer.env | nindent 12 }}
            {{- end }}
            {{- if .Values.global.secrets }}
            - name: CRAFTPLAN_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "craftplan.secretName" . }}
                  key: mcpServer.craftplanToken
            - name: MCP_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "craftplan.secretName" . }}
                  key: mcpServer.mcpSecret
            {{- end }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          {{- if .Values.mcpServer.resources }}
          resources:
            {{- toYaml .Values.mcpServer.resources | nindent 12 }}
          {{- end }}
          {{- if .Values.mcpServer.volumeMounts }}
          volumeMounts:
            {{- toYaml .Values.mcpServer.volumeMounts | nindent 12 }}
          {{- end }}
          {{- if .Values.mcpServer.configMap }}
          volumeMounts:
            - name: config
              mountPath: /etc/mcp-server/config
              readOnly: true
          {{- end }}
          {{- if .Values.mcpServer.secrets }}
          volumeMounts:
            - name: secrets
              mountPath: /etc/mcp-server/secrets
              readOnly: true
          {{- end }}
          {{- if .Values.mcpServer.persistence.enabled }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
      {{- if .Values.mcpServer.volumes }}
      volumes:
        {{- toYaml .Values.mcpServer.volumes | nindent 8 }}
      {{- end }}
      {{- if .Values.mcpServer.configMap }}
      volumes:
        - name: config
          configMap:
            name: {{ include "craftplan.configMapName" . }}-mcp
      {{- end }}
      {{- if .Values.mcpServer.secrets }}
      volumes:
        - name: secrets
          secret:
            secretName: {{ include "craftplan.secretName" . }}
      {{- end }}
      {{- if .Values.mcpServer.persistence.enabled }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "craftplan.pvcName" . }}-mcp
      {{- end }}
      {{- if .Values.global.costOptimization.resourceLimits.enabled }}
      resources:
        limits:
          cpu: {{ .Values.global.costOptimization.resourceLimits.maxCPU }}
          memory: {{ .Values.global.costOptimization.resourceLimits.maxMemory }}
      {{- end }}
      {{- if .Values.global.security.networkPolicy.enabled }}
      networkPolicy:
        enabled: {{ .Values.global.security.networkPolicy.enabled }}
        {{- if .Values.global.security.networkPolicy.ingress }}
        ingress:
          {{- toYaml .Values.global.security.networkPolicy.ingress | nindent 10 }}
        {{- end }}
        {{- if .Values.global.security.networkPolicy.egress }}
        egress:
          {{- toYaml .Values.global.security.networkPolicy.egress | nindent 10 }}
        {{- end }}
      {{- end }}
{{- end }}