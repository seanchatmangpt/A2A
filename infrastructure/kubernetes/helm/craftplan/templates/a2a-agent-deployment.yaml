{{- if .Values.a2aAgent.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "craftplan.fullname" . }}-a2a
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "craftplan.labels" . | nindent 4 }}
    app.kubernetes.io/name: a2a-agent
  annotations:
    {{- if .Values.global.deploymentStrategy }}
    deployment.kubernetes.io/revision: "{{ .Values.global.deploymentStrategy.revision | default 1 }}"
    {{- end }}
    {{- if .Values.a2aAgent.annotations }}
    {{- toYaml .Values.a2aAgent.annotations | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ include "craftplan.replicaCount" . | default .Values.a2aAgent.replicas }}
  {{- if .Values.a2aAgent.strategy }}
  strategy:
    {{- toYaml .Values.a2aAgent.strategy | nindent 4 }}
  {{- else }}
  strategy:
    type: {{ .Values.global.deploymentStrategy.type | default "RollingUpdate" }}
    rollingUpdate:
      maxSurge: {{ .Values.global.deploymentStrategy.rollingUpdate.maxSurge | default "25%" }}
      maxUnavailable: {{ .Values.global.deploymentStrategy.rollingUpdate.maxUnavailable | default "0" }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "craftplan.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: a2a-agent
  template:
    metadata:
      labels:
        {{- include "craftplan.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/name: a2a-agent
        app.kubernetes.io/component: a2a-agent
        {{- if .Values.a2aAgent.podLabels }}
        {{- toYaml .Values.a2aAgent.podLabels | nindent 8 }}
        {{- end }}
      annotations:
        {{- if .Values.a2aAgent.podAnnotations }}
        {{- toYaml .Values.a2aAgent.podAnnotations | nindent 8 }}
        {{- end }}
        checksum/config: {{ include "craftplan.config" . | sha256sum }}
        checksum/secrets: {{ include "craftplan.secret" . | sha256sum }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: {{ include "craftplan.metricsPath" . }}
        {{- if .Values.a2aAgent.monitoring.enabled }}
        kubernetes.io/monitoring: "true"
        {{- end }}
    spec:
      {{- if .Values.a2aAgent.priorityClassName }}
      priorityClassName: {{ .Values.a2aAgent.priorityClassName }}
      {{- end }}
      {{- if .Values.a2aAgent.serviceAccount.name }}
      serviceAccountName: {{ include "craftplan.serviceAccountName" . }}
      {{- end }}
      {{- include "craftplan.imagePullSecret" . | nindent 6 }}
      {{- if .Values.a2aAgent.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.a2aAgent.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.a2aAgent.tolerations }}
      tolerations:
        {{- toYaml .Values.a2aAgent.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.a2aAgent.affinity }}
      affinity:
        {{- toYaml .Values.a2aAgent.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.global.costOptimization.spotInstances.enabled }}
      nodeSelector:
        {{- toYaml .Values.global.costOptimization.spotInstances.nodeSelector | nindent 8 }}
      tolerations:
        - key: "spot"
          operator: "Equal"
          value: "true"
          effect: "NoSchedule"
      {{- end }}
      securityContext:
        {{- include "craftplan.securityContext" . | nindent 8 }}
      containers:
        - name: a2a-agent
          image: "{{ .Values.a2aAgent.image.repository }}:{{ .Values.a2aAgent.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.a2aAgent.image.pullPolicy }}
          securityContext:
            {{- include "craftplan.securityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          {{- if .Values.a2aAgent.livenessProbe }}
          livenessProbe:
            {{- toYaml (include "craftplan.livenessProbe" .) | nindent 12 }}
          {{- end }}
          {{- if .Values.a2aAgent.readinessProbe }}
          readinessProbe:
            {{- toYaml (include "craftplan.readinessProbe" .) | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.a2aAgent.env }}
            {{- toYaml .Values.a2aAgent.env | nindent 12 }}
            {{- end }}
            {{- if .Values.global.secrets }}
            - name: MCP_SERVER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "craftplan.secretName" . }}
                  key: a2aAgent.mcpToken
            {{- end }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          {{- if .Values.a2aAgent.resources }}
          resources:
            {{- toYaml .Values.a2aAgent.resources | nindent 12 }}
          {{- end }}
          {{- if .Values.a2aAgent.volumeMounts }}
          volumeMounts:
            {{- toYaml .Values.a2aAgent.volumeMounts | nindent 12 }}
          {{- end }}
          {{- if .Values.a2aAgent.configMap }}
          volumeMounts:
            - name: config
              mountPath: /etc/a2a-agent/config
              readOnly: true
          {{- end }}
          {{- if .Values.a2aAgent.secrets }}
          volumeMounts:
            - name: secrets
              mountPath: /etc/a2a-agent/secrets
              readOnly: true
          {{- end }}
          {{- if .Values.a2aAgent.persistence.enabled }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
      {{- if .Values.a2aAgent.volumes }}
      volumes:
        {{- toYaml .Values.a2aAgent.volumes | nindent 8 }}
      {{- end }}
      {{- if .Values.a2aAgent.configMap }}
      volumes:
        - name: config
          configMap:
            name: {{ include "craftplan.configMapName" . }}-a2a
      {{- end }}
      {{- if .Values.a2aAgent.secrets }}
      volumes:
        - name: secrets
          secret:
            secretName: {{ include "craftplan.secretName" . }}
      {{- end }}
      {{- if .Values.a2aAgent.persistence.enabled }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "craftplan.pvcName" . }}-a2a
      {{- end }}
      {{- if .Values.global.costOptimization.resourceLimits.enabled }}
      resources:
        limits:
          cpu: {{ .Values.global.costOptimization.resourceLimits.maxCPU }}
          memory: {{ .Values.global.costOptimization.resourceLimits.maxMemory }}
      {{- end }}
      {{- if .Values.global.security.networkPolicy.enabled }}
      networkPolicy:
        enabled: {{ .Values.global.security.networkPolicy.enabled }}
        {{- if .Values.global.security.networkPolicy.ingress }}
        ingress:
          {{- toYaml .Values.global.security.networkPolicy.ingress | nindent 10 }}
        {{- end }}
        {{- if .Values.global.security.networkPolicy.egress }}
        egress:
          {{- toYaml .Values.global.security.networkPolicy.egress | nindent 10 }}
        {{- end }}
      {{- end }}
{{- end }}